numpy>=1.24
networkx>=3.1
matplotlib>=3.7
pytest>=7.0
# music21 is only needed to rebuild the showcase pkl (scripts/build_showcase_pkl.py);
# the core pipeline does not import it at runtime.
music21>=9.0
